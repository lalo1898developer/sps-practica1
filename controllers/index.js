// Importanmos los metodos a una constante de nuestro controlador
const ArticleController = require('./ArticleController');

// Exportamos esta constante para que sean usados los metodos que contiene por los endpoints
module.exports = {
    ArticleController,
};